{% extends 'admin/base.html.twig' %}
{% block title %}
    <title>Mon Profil</title>
{% endblock %}
{% set menu = [
    ["Annuler","app_annuler"],
] %}
{% block body %}

<div class="content-container mx-2">
    <div class="container-fluid">
        <div class="jumbotron">
            <h1 class="">Enregistrement du profil</h1>

        </div>
    <div class="row text-center mt-4">

    </div>
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="row mt-3">
                    <div>
                        {% form_theme formProfil 'bootstrap_5_horizontal_layout.html.twig' %}
                        {{ form(formProfil) }}
                    </div>
                </div>
            </div>
{#            {{ form_start(form, {'action': path('target_route'), 'method': 'GET'}) }}#}

        </div>
    </div>
    </div>
{% endblock %}